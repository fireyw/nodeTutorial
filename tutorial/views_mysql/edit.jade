doctype html
html
    head
        meta(charset='utf-8')
    body
        h1
            a(href='/topic') edit
        ul
            each topic in topics
                li
                    - id = topic.id
                    a(href='/topic/' + id)=topic.title
        article
            form(action='/topic/'+id+'/edit', method='post')
                p
                input(type='text' name='title' placeholder='title' value=topic.title)
                p
                textarea(name='des' placeholder='des' )=topic.des
                p
                input(type='text' name='author' placeholder='author' value=topic.author)
                p
                input(type='submit')
