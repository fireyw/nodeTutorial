// =는 변수값을 출력 한다고 뜻함
doctype html
html
    head
        meta(charset='utf-8')
    body
        h1
            a(href='/topic') Sever Side JavaScript
        ul
           each topic in topics
                li
                    - rid = encodeURIComponent(topic['@rid'])
                    a(href='/topic/'+rid)=topic.NPM
        article
        if topic
            h2=topic.NPM
            =topic.Javascript
        else
            h2 Welcome
            | This is server side javascript
        div
            a(href='/topic/new') new
